const { randomNumber, openRangedRandomNumber } = require('./gen-random-number');
const randomString = require('./gen-random-string');
const randomArrayElement = require('./get-random-array-element');

module.exports = {
  randomNumber,
  openRangedRandomNumber,
  randomString,
  randomArrayElement
};

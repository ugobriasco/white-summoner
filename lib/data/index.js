const countries = require("./country-list");
const names = require("./names-list");
const mccs = require("./mccs-available");
const countriesAvailable = require("./countries-available");

module.exports = { countries, names, mccs, countriesAvailable };
